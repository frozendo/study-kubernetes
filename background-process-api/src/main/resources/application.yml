server:
  port: 9000

spring:
  application:
    name: study-kubernetes
  datasource:
    url: jdbc:postgresql://localhost:5432/kubernetes-db
    username: root
    password: 123@qwe#
    poolName: kubernetes-pool
    maximum-pool-size: 10
    minimum-idle: 0
    idle-timeout: 60000
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false